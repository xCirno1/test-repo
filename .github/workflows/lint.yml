name: lint

on:
  push:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Run Pyright
        uses: jakebailey/pyright-action@v1
        with:
          version: '1.1.286'
          warnings: false
